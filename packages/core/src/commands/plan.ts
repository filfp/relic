export interface PlanOptions {
  relicDir: string;
}

export async function runPlan(_options: PlanOptions): Promise<void> {
  console.log("relic plan — not yet implemented.");
  console.log("Use the /relic:plan command in your AI agent.");
}
