export interface TasksOptions {
  relicDir: string;
}

export async function runTasks(_options: TasksOptions): Promise<void> {
  console.log("relic tasks — not yet implemented.");
  console.log("Use the /relic:tasks command in your AI agent.");
}
