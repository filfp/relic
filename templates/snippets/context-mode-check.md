Run:

```bash
relic context
```

Read the `mode` field from the output:

- `mode = "md"` — all artifacts this command produces or updates use Markdown (`.md`).
- `mode = "html"` — all artifacts this command produces or updates use HTML (`.html`), authored with `<relic-*>` components.

**Commit to one mode for the entire session — do not switch mid-session.**
