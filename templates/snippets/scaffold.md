```bash
relic scaffold --title "<derived title>"
```

This generates the spec ID, creates the folder, and writes empty scaffolding for `spec.md`, `plan.md`, `tasks.md`, and `artifacts.json`. Use the `spec_id` from the JSON output for all subsequent file references — do not infer it from anywhere else.
